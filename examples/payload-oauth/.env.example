# Payload CMS OAuth Example - Environment Variables
# Copy this file to .env.local and fill in your values

# =============================================================================
# Required: OAuth Configuration
# =============================================================================

# WorkOS JWKS URI for token verification
# Get this from your WorkOS dashboard: https://dashboard.workos.com
WORKOS_JWKS_URI=https://api.workos.com/sso/jwks/client_XXX

# Payload secret for signing (generate a random 32+ character string)
PAYLOAD_SECRET=your-secret-key-change-in-production

# =============================================================================
# Optional: OAuth Configuration
# =============================================================================

# OAuth client ID (used to verify audience claim in JWT)
# OAUTH_CLIENT_ID=your-client-id

# Cookie name for the auth token (default: 'auth')
# OAUTH_COOKIE_NAME=auth

# Roles that grant admin access (comma-separated)
# OAUTH_ADMIN_ROLES=admin

# Roles that grant editor access (comma-separated)
# OAUTH_EDITOR_ROLES=editor

# =============================================================================
# Optional: Development
# =============================================================================

# Data directory for local development (default: ./data)
# DATA_DIR=./data

# Server URL (default: http://localhost:3000)
# PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# =============================================================================
# Testing: These are set automatically by the test setup
# =============================================================================

# For E2E tests, these are set by global-setup.ts:
# WORKOS_JWKS_URI=http://localhost:3456/.well-known/jwks.json
# TEST_ADMIN_TOKEN=<generated>
# TEST_EDITOR_TOKEN=<generated>
# TEST_USER_TOKEN=<generated>
